{
  "name": "recall-a-palooza-backend",
  "version": "1.0.0",
  "description": "The backend service to serve data to and from the database",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.2",
    "node-cron": "^3.0.3",
    "nodemon": "^3.0.1"
  }
}
